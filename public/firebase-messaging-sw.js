// Placeholder service worker to avoid 404 noise from old registrations or browser extensions.
self.addEventListener('install', () => {
  self.skipWaiting();
});

self.addEventListener('activate', (event) => {
  event.waitUntil(self.clients.claim());
});
